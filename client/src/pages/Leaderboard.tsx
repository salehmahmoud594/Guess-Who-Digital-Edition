// DESIGN PHILOSOPHY: Whimsical Tabletop Editorial — the leaderboard feels like a hand-kept scorecard, not a data grid.
import { ArrowLeft, BarChart3, Medal } from "lucide-react";
import { useLocation } from "wouter";
import { AppFrame } from "@/components/game/AppFrame";
import { BrandMark } from "@/components/game/BrandMark";
import { readLeaderboard, type LeaderboardEntry } from "@/lib/leaderboard";
import { useEffect, useState } from "react";
export default function Leaderboard() { const [, navigate] = useLocation(); const [entries, setEntries] = useState<LeaderboardEntry[]>([]); useEffect(() => setEntries(readLeaderboard()), []); return <AppFrame eyebrow="Local scorecard" title="Who’s on top?" action={<button className="icon-button" type="button" onClick={() => navigate("/")} aria-label="Back to home"><ArrowLeft size={18} /></button>}><div className="leaderboard-page"><div className="leaderboard-intro"><span className="ink-tab">The long game</span><h1>Lifetime<br /><em>wins.</em></h1><p>Every correct guess gets a line on this device’s scorecard. No accounts, no cloud, just bragging rights at the table.</p></div><div className="scorecard"><div className="scorecard-head"><span><BarChart3 size={17} /> Player</span><span>Wins</span></div>{entries.length === 0 ? <div className="empty-scorecard"><span className="empty-medal"><Medal size={24} /></span><strong>No games played yet!</strong><p>Make the first correct guess and your name will land here.</p><button className="primary-button" type="button" onClick={() => navigate("/setup")}>Play the first round</button></div> : entries.map((entry, index) => <div className="score-row" key={`${entry.name}-${index}`}><span className="rank">{String(index + 1).padStart(2, "0")}</span><span className="score-player"><span className={`score-avatar score-avatar-${index % 4}`}>{entry.name.slice(0, 1).toUpperCase()}</span><strong>{entry.name}</strong></span><strong className="score-wins">{entry.wins}<small>{entry.wins === 1 ? " win" : " wins"}</small></strong></div>)}</div><div className="leaderboard-footer"><BrandMark compact /><span>Local only · stored on this browser</span></div></div></AppFrame>; }
