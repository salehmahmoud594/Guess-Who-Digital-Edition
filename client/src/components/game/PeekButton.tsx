// DESIGN PHILOSOPHY: Whimsical Tabletop Editorial — a private peek behaves like lifting a card corner, with a deliberate hold gesture.
import { Eye, EyeOff } from "lucide-react";
export function PeekButton({ playerName, visible, onStart, onEnd }: { playerName: string; visible: boolean; onStart: () => void; onEnd: () => void }) { return <button className={`peek-button ${visible ? "peek-active" : ""}`} type="button" onPointerDown={onStart} onPointerUp={onEnd} onPointerLeave={onEnd} onKeyDown={(event) => { if (event.key === "Enter" || event.key === " ") onStart(); }} onKeyUp={(event) => { if (event.key === "Enter" || event.key === " ") onEnd(); }} aria-label={`Hold to peek at ${playerName}'s secret`}><span className="peek-icon">{visible ? <EyeOff size={17} /> : <Eye size={17} />}</span>{visible ? "Release to hide" : "Hold to peek"}</button>; }
