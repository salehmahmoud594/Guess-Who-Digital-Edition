// DESIGN PHILOSOPHY: Whimsical Tabletop Editorial — privacy is a physical curtain: opaque, direct, and impossible to misread.
import { LockKeyhole, ArrowRight } from "lucide-react";
export function HandoffOverlay({ nextPlayerName, reason, onContinue }: { nextPlayerName: string; reason: "secret" | "turn"; onContinue: () => void }) { return <div className="handoff-overlay" role="dialog" aria-modal="true" aria-labelledby="handoff-title"><div className="handoff-card"><span className="handoff-lock"><LockKeyhole size={26} /></span><span className="eyebrow">Private handoff</span><h2 id="handoff-title">Pass the device to<br /><em>{nextPlayerName}</em></h2><p>{reason === "secret" ? "Your secret is safely hidden. Their turn to choose is next." : "The last board is covered. Take a look only when the device is in your hands."}</p><button className="primary-button" type="button" onClick={onContinue}>I have the device <ArrowRight size={18} /></button></div></div>; }
