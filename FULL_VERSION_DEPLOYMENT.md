# تشغيل النسخة الكاملة من Guess Who: Digital Edition

## الفرق بين المصدر والاستضافة

يمكن حفظ **كامل مصدر المشروع** في مستودع GitHub: الواجهة، خادم Express، إجراءات tRPC، منطق Room، الاختبارات، وبناء GitHub Pages المحلي. لا تُرفع ملفات البيئة أو مفاتيح Supabase أو كلمة مرور اللعبة إلى المستودع.

لكن GitHub Pages يستضيف ملفات ثابتة فقط. لذلك فهو مناسب للنسخة المحلية ذات لاعبَين على جهاز واحد، ولا يمكنه تشغيل `Room` لأنه لا يشغّل خادم Node/Express ولا إجراءات tRPC ولا ملفات تعريف الارتباط الآمنة ولا مهام التنظيف الدورية.

## الخيار الموصى به حاليًا

تظل النسخة الكاملة منشورة على الاستضافة الحالية للمشروع، بينما يبقى GitHub هو سجل المصدر والنسخة الثابتة القابلة للتشغيل محليًا. لا يحتاج هذا المسار إلى إعداد جديد من المستخدم، وتبقى الأسرار مضبوطة على الخادم فقط.

## نقل Room إلى استضافة خارجية

أي بديل خارجي يحتاج منصة تستطيع تشغيل عملية Node.js وتهيئة متغيرات بيئة سرية من لوحة إعدادات المنصة. يجب ضبط القيم التالية هناك، من دون كتابتها في Git أو في JavaScript الخاص بالمتصفح:

| المتغير | الاستخدام |
| --- | --- |
| `GAME_ACCESS_PASSWORD` | التحقق الخادمي من كلمة مرور الدخول المشتركة. |
| `JWT_SECRET` | توقيع جلسة دخول اللعبة عبر ملف تعريف ارتباط آمن. |
| `SUPABASE_URL` | عنوان مشروع Supabase. |
| `SUPABASE_SERVICE_ROLE_KEY` | وصول الخادم إلى بيانات Rooms عبر Supabase REST؛ لا يجوز كشفه للمتصفح. |

قد تحتاج الاستضافة إلى متغيرات إضافية إذا أردت الإبقاء على تكاملات المنصة الحالية، مثل `VITE_APP_ID` و`OAUTH_SERVER_URL`. أمّا تنظيف الغرف الخاملة فيجب أن يُشغَّل عبر جدولة المنصة الخارجية أو عبر خدمة مجدولة مستقلة؛ مهمة الجدولة الحالية مرتبطة بالاستضافة الحالية للمشروع.

## قائمة تحقق قبل النقل

1. ارفع المصدر فقط، وتحقق أن `.env` لا يدخل في أي commit.
2. اختر استضافة تدعم Node.js والخدمات المجدولة، لا استضافة ثابتة فقط.
3. أضف الأسرار من لوحة الاستضافة، ثم اختبر الدخول بكلمة المرور وإنشاء Room والانضمام من جهاز ثانٍ.
4. اضبط عنوان الإنتاج ضمن إعدادات Supabase والأصل المسموح به إذا لزم، ثم شغّل فحص Room الثنائي بعد النشر.

## النشر الثابت على GitHub Pages

يبقى أمر `pnpm build:github-pages` مخصصًا لتوليد نسخة محلية فقط. لا يستورد هذا البناء واجهات Room أو tRPC أو مفاتيح الخادم، لحماية الخصوصية ومنع رابط GitHub Pages من إظهار تجربة لا يمكنه تشغيلها.
