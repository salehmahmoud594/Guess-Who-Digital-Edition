# تصميم اللعب عبر جهازين — Guess Who: Digital Edition

> **الحالة:** مسودة معمارية للموافقة. لا توجد في هذه الوثيقة أي تغييرات تنفيذية على اللعبة أو الخادم.

## 1. نطاق النسخة الأولى

ستظل اللعبة الحالية كما هي، مع الاحتفاظ بوضع اللعب على جهاز واحد. عند بدء اللعبة يختار المستخدم بين وضعين مستقلين:

| الوضع | الاستخدام | مصدر الحالة |
|---|---|---|
| جهاز واحد | Pass & Play على الهاتف أو Split Screen على الشاشة الكبيرة | الحالة الحالية داخل المتصفح و`localStorage` للـleaderboard فقط |
| Room | جهاز لكل لاعب، بدون صوت أو chat | خادم مركزي يحفظ حالة الغرفة ويزامن الجهازين |

النسخة الأولى من Room تدعم **منشئًا واحدًا وضيفًا واحدًا فقط**. لا توجد spectators أو حسابات أو دعوات اجتماعية أو دردشة.

## 2. القرارات المعتمدة

يدخل اللاعبان باستخدام **كود غرفة فقط**، ولا يحتاج أي منهما إلى تسجيل حساب. منشئ الغرفة هو اللاعب الذي يختار التصنيف والإعدادات، بينما يدخل الضيف إلى الإعدادات التي اختارها المنشئ ولا يستطيع تغييرها.

تتم المزامنة بتحديثات قصيرة كل **1–2 ثانية** في النسخة الأولى. كل حركة مهمة تُرسل كـcommand إلى الخادم، والخادم يتحقق من صلاحيتها ثم يعيد snapshot مخصصًا لكل مقعد. لا يرسل الخادم snapshot واحدًا يحتوي أسرار اللاعبَين إلى كلا الجهازين.

## 3. المسار المقترح للمستخدم

يظهر في الشاشة الرئيسية زران واضحان: **Play on one device** و **Create or join a Room**. مسار الجهاز الواحد يستمر في استخدام الـreducer الحالي دون تحويله إلى اتصال شبكي.

في مسار Room، ينشئ اللاعب الأول غرفة ويحصل على كود قصير من ستة أحرف ورابط مشاركة اختياري. تظهر شاشة انتظار حتى يدخل اللاعب الثاني. بعد الانضمام يرى الطرفان أسماء المقاعد وحالة الاتصال، ثم يبدأ المنشئ الجولة بعد أن يصبح الطرفان جاهزين.

يختار كل لاعب سره من جهازه. بعد تثبيت السر ينتقل الطرفان إلى شاشة اللعب البعيدة، حيث يرى كل لاعب لوحه وحالته الخاصة، بينما تظهر الأحداث العامة فقط. عند انتهاء الجولة تظهر النتيجة للطرفين، ويمكن للمنشئ بدء جولة جديدة أو العودة إلى الشاشة الرئيسية.

## 4. حدود الخصوصية

تُعامل بيانات Room كبيانات مقسّمة حسب المقعد، وليست كحالة كاملة يمكن للعميل قراءتها. projection الخادم يعتمد على `seat` قبل إرسال الرد:

| البيانات | Player 1 | Player 2 |
|---|---:|---:|
| كتالوج البطاقات العام | يرى | يرى |
| إعدادات الغرفة والتصنيف | يرى | يرى |
| السر الخاص | يرى | لا يرى |
| السر الخاص بالخصم | لا يرى | يرى |
| مجموعة الاستبعاد الخاصة | يرى | لا يرى |
| القلوب الخاصة | يرى | لا يرى |
| الدور الحالي وحالة الغرفة | يرى | يرى |
| نتيجة التخمين بعد اعتمادها | يرى | يرى |

الأسرار، مجموعات الاستبعاد، والقلوب لا تُرسل في endpoint عام. حتى لو عدّل المستخدم JavaScript في المتصفح، يجب أن يرفض الخادم أي command لا يطابق المقعد والدور والمرحلة الحالية.

## 5. نموذج الحالة المقترح

الغرفة تحتوي على `roomCode`، و`status`، و`settings`، و`deckSeed`، و`revision`، ووقت آخر نشاط. لكل مقعد تحفظ الحالة الخاصة به: `secretCardId`، و`eliminatedIds`، و`hearts`، و`ready`، و`lastSeenAt`.

الحالات الرئيسية للغرفة هي: `waiting`، ثم `setup`، ثم `secret_selection`، ثم `playing`، ثم `finished`، ثم `expired`. التغييرات لا تُطبّق مباشرة من الواجهة؛ بل تمر عبر انتقالات مسموحة مرتبطة بالحالة الحالية.

## 6. الأوامر والمزامنة

الواجهة تستخدم استدعاءً موحدًا لجلب snapshot المقعد الحالي، وتنفذ polling كل 1500 مللي ثانية تقريبًا مع إيقافه عند مغادرة الصفحة. الأوامر الأساسية ستكون:

| الأمر | من يحق له | التحقق الأساسي |
|---|---|---|
| `joinRoom` | ضيف غير معيّن | الكود صحيح والغرفة بها مقعد متاح |
| `updateSettings` | المنشئ فقط | الحالة `waiting` أو `setup` |
| `setReady` | المقعد نفسه | الغرفة لم تبدأ اللعب |
| `selectSecret` | المقعد نفسه | البطاقة ضمن deck ولم تثبت سابقًا |
| `toggleElimination` | اللاعب صاحب المقعد | الدور والبطاقة والحالة صحيحة |
| `submitGuess` | اللاعب صاحب الدور | التخمين يخص بطاقة موجودة ولم تُحسم الجولة |
| `startRematch` | المنشئ بعد النهاية | كلا المقعدين موجودان |

كل snapshot يحمل `revision`. إذا أرسل عميل command بإصدار قديم، يعيد الخادم أحدث snapshot بدل تطبيق حركة قديمة. هذا يمنع التحديثات المتأخرة من إعادة حالة اللعبة للخلف.

## 7. refresh وإعادة الاتصال

بعد إنشاء الغرفة أو الانضمام إليها، يحصل الجهاز على `seatToken` عشوائي خاص بالمقعد. يخزن التطبيق token في `sessionStorage` فقط، بحيث ينجح refresh في نفس التبويب من دون جعل المقعد قابلًا للاسترجاع من جهاز آخر.

يخزن الخادم hash للـtoken مع `seat` و`expiresAt` و`lastSeenAt`. إذا عاد اللاعب بالـtoken الصحيح خلال فترة الصلاحية، يستعيد المقعد وحالته الخاصة. إذا انقطع الاتصال، تظهر طبقة opaque بعنوان **Reconnecting**، وتستمر الغرفة في الانتظار فترة قصيرة بدل إنهاء الجولة فورًا.

لمنع تضارب التبويبات، يرتبط token بمعرّف تبويب محفوظ في `sessionStorage`. فتح تبويب آخر بالـtoken نفسه يعرض رسالة بأن المقعد مفتوح في تبويب آخر بدل السماح بتحديثين متنافسين. تنتهي الغرفة تلقائيًا بعد فترة انتظار طويلة أو بعد مدة خمول معقولة أثناء اللعب.

## 8. البنية التقنية المقترحة

المشروع الحالي static-only، لذلك يحتاج Room إلى ترقية إلى مشروع full-stack يضيف backend وقاعدة بيانات وإجراءات typed. في النسخة الأولى لا نحتاج WebSocket أو خدمة خارجية؛ قاعدة بيانات مع polling كل 1–2 ثانية أبسط، وتناسب طبيعة لعبة الأسئلة بالتبادل، وتسمح ببقاء الاستضافة المعتادة دون خادم realtime دائم.

يبقى منطق اللعبة الحتمي في `gameEngine` و`GameContext` مصدرًا مشتركًا لمعادلات الجولة، بينما يصبح الخادم هو السلطة على أوامر Room وانتقالات الحالة. مسار الجهاز الواحد لا يستدعي الـbackend، وبالتالي لا يتأثر إذا لم يرد المستخدم اللعب عبر الإنترنت.

## 9. خارج نطاق النسخة الأولى

لن تشمل النسخة الأولى الصوت أو الفيديو أو chat أو spectators أو حسابات المستخدمين أو مشاركة leaderboard بين الأجهزة أو peer-to-peer networking. يمكن إضافة realtime دائم لاحقًا إذا أصبح تأخير 1–2 ثانية مزعجًا، من دون تغيير نموذج الخصوصية أو أوامر اللعبة.

## 10. ترتيب التنفيذ بعد الموافقة

1. ترقية المشروع إلى full-stack مع backend وقاعدة بيانات.
2. إنشاء schema للغرف والمقاعد وtokens وsnapshot الخاص بكل مقعد.
3. إضافة إجراءات إنشاء الغرفة والانضمام والجاهزية والاختيار واللعب وإعادة الاتصال، مع اختبارات انتقالات الحالة والخصوصية.
4. إضافة شاشة اختيار الوضع، وشاشات Create/Join/Waiting، ثم واجهة Remote Gameplay.
5. اختبار وضع الجهاز الواحد regression، ثم اختبار جهازين منفصلين مع refresh وانقطاع مؤقت وانتهاء الغرفة.

لن يبدأ أي تعديل على المشروع أو إضافة backend إلا بعد اعتماد هذا التصميم أو تعديل النقاط التي تريد تغييرها.
